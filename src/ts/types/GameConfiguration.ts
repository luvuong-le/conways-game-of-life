type GameConfiguration {
    canvas: HTMLCanvasElement,
    canvasWidth: number,
    canvasHeight: number,
    iterationCount: HTMLElement,
    cellColorRandomiser: HTMLElement,
    theme: HTMLElement,
    timerEnabled: HTMLElement
};

export default GameConfiguration;